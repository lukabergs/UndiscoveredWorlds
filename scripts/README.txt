Check if the developed scripts still carry value for the current repo architecture.

Review (2026-09-03):
refs/ contains reusable reference download and preparation tools; benchmarks/ contains observed-climate checks and workbook aggregation. Paths and the moved Python test import are repaired. Tools whose old terrain/reference defaults were deleted now require explicit compatible inputs. check-layout.py provides a quick read-only layout audit. The one-off repo_cleanup/ scripts and migration receipts were moved to archive/repo_cleanup/.
